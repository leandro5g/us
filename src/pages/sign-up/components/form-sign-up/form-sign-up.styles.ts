import styled from "styled-components/native";

export const Container = styled.View``;

export const Content = styled.View`
  margin: ${({ theme }) => theme.METRICS.PADDING_VERTICAL * 2.5}px 0px;
`;
